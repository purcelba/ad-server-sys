# stream_features

Not yet built — owned by **Phase 2** (`phases.md`).

Will hold the Redpanda event consumer, windowed session aggregates
written to Redis with TTL, the streaming handler interface
(`event_type()` / `outputs()` / `update()`), and unknown-event tolerance.
