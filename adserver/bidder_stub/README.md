# bidder_stub

Not yet built — owned by **Phase 5** (`phases.md`).

Will hold the fake external programmatic bidder: configurable latency
distribution and failure rate, called by the ad server with a hard 30ms
timeout.
