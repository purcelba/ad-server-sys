# adserver (serving endpoint)

Not yet built — owned by **Phase 5** (`phases.md`).

Will hold the `POST /serve` endpoint: candidate retrieval, feature fetch,
scoring, external demand (bidder), yield arbitration, the degradation
ladder, the decision log, and A/B assignment.
