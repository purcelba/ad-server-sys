# common

Not yet built — owned by **Phase 1** (`phases.md`).

Will hold shared schemas, the feature registry loader (`registry.yaml`),
and metrics helpers. Per `CLAUDE.md`, this is the only code other
components may import across component boundaries.
