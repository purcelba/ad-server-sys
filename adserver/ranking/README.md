# ranking

Not yet built — owned by **Phase 4** (`phases.md`).

Will hold the pCTR training pipeline (`train.py`), the model artifact
contract, the model registry (`registry.json`), promotion/rollback
(`promote.py`), the scorer (`scorer.py`), and shared cross-feature
functions (`common/crosses.py`).
