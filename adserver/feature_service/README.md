# feature_service

Not yet built — owned by **Phase 3** (`phases.md`).

Will hold the FastAPI feature retrieval API (`POST /features`), reading
Redis first and falling back to DynamoDB-local, with freshness-status
metadata and a committed OpenAPI schema-drift test.
