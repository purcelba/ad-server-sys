# batch_features

Not yet built — owned by **Phase 1** (`phases.md`).

Will hold the Polars batch feature jobs (feature-job interface: `outputs()`
+ `compute(date_range)`), the offline Parquet/DuckDB store, the
materialization job to DynamoDB-local, the data-quality gate, and audience
evaluation (`audiences.yaml`).
